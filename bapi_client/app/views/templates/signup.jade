//This script's id tells the component which template to pick up & render   
script(type="text/x-template" id="sign-up")
    .col-xs-10.col-xs-offset-1.col-md-6.col-md-offset-3
        .jumbotron
            .alert(v-bind:class="{'alert-danger': !formFilled, 'alert-success': formFilled }") {{ responseMessage }}
            form
                .form-group
                    label(for="email") Email address
                    input.form-control(type="email" placeholder="Your email" v-model="email")
                .form-group
                    label(for="username") Username
                    input.form-control(type="text" placeholder="Your username" v-model="username")
                .form-group
                    label(for="password]") Password
                    input.form-control(type="password" placeholder="Your password" v-model="password")
                
                button(type="submit" v-on:click.prevent.stop="registerUser").btn.btn-default Submit
                
            router-link(to="sign-in") Already a user? Click her to sign in.
